spring:
  application:
    name: remember
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/remember_app?useUnicode=true&characterEncoding=utf-8&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 111111
    driver-class-name: com.mysql.cj.jdbc.Driver
  jackson:
    property-naming-strategy: SNAKE_CASE

mybatis-plus:
  mapper-locations: classpath*:mapper/*.xml